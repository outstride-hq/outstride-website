/**
 * Outstride OS public API — re-exports types, data, status helpers and queries.
 *
 * Import from `@/lib/os` in app code. Edit taxonomy in `os-data.ts` and
 * rich page content in `tools-content.ts` / `capabilities-content.ts`.
 */

export type * from "@/lib/os-types";
export * from "@/lib/os-data";
export * from "@/lib/os-status";
export * from "@/lib/os-queries";
